<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_photos"
        android:layout_width="match_parent"
        android:layout_height="665dp"
        android:background="@android:color/background_light"
        android:paddingTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/game_prompt">

    </androidx.recyclerview.widget.RecyclerView>

    <TextView
        android:id="@+id/game_prompt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/background_light"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:gravity="center"
        android:textSize="20sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Who is so-and-so?" />

</androidx.constraintlayout.widget.ConstraintLayout>

<!-- TODO Add layout for landscape mode - now that it's a grid, how would that be done?-->
<!-- TODO Add layout for larger screens (tablet looks weird) -->